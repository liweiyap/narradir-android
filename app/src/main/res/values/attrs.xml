<?xml version="1.0" encoding="utf-8"?>
<resources>
    <declare-styleable name="CustomFont">
        <attr name="assetFontPath" format="string"/>
    </declare-styleable>

    <declare-styleable name="StrokedTextAttrs">
        <attr name="textStrokeColor" format="color"/>
        <attr name="textStrokeWidth" format="float"/>
    </declare-styleable>

    <declare-styleable name="CheckableInitHelper">
        <attr name="defaultCheckedState" format="boolean"/>  <!-- Seems like android:state_checked does nothing during initialisation in XML? -->
        <attr name="alphaUnchecked" format="float"/>
    </declare-styleable>
</resources>