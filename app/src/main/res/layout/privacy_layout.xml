<?xml version="1.0" encoding="utf-8"?>
<!-- LinearLayout here is used as a wrapper to pass in wrap_content rather than match_parent to android:layout_height -->
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    style="@style/RowLayoutStyleDefault"
    android:id="@+id/privacyLayout">

    <com.liweiyap.narradir.ui.fonts.CustomTypefaceableObserverButton
        android:id="@+id/privacyButton"
        android:text="@string/privacy_button_text"
        android:layout_marginHorizontal="25dp"
        style="@style/NavBarCapsuleStyle" />

</LinearLayout>